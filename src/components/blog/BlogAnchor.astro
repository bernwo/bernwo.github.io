---
const { href } = Astro.props;
const anchorClass = "blog-content-anchor coloured-link inline-block";
---

<a href={href} target="_blank" rel="noopener noreferrer" class={anchorClass}
    ><slot /></a
>
